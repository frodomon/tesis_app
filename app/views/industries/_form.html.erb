<%= form_for(@industry) do |f| %>
  <% if @industry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@industry.errors.count, "error") %> prohibited this industry from being saved:</h2>

      <ul>
      <% @industry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="parent">
      <label>Linea Padre:</label>
      <select id="parent">
        <option value = "0" selected>Sin Padre</option>
      </select>
  </div>
  <div class="field">
    <%= f.number_field :parent_id , id:"parent_id"%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
