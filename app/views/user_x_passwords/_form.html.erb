<%= form_for(@user_x_password) do |f| %>
  <% if @user_x_password.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_x_password.errors.count, "error") %> prohibited this user_x_password from being saved:</h2>

      <ul>
      <% @user_x_password.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :password_id %><br>
    <%= f.number_field :password_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
