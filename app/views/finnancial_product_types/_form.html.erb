<%= form_for(@finnancial_product_type) do |f| %>
  <% if @finnancial_product_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@finnancial_product_type.errors.count, "error") %> prohibited this finnancial_product_type from being saved:</h2>

      <ul>
      <% @finnancial_product_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
